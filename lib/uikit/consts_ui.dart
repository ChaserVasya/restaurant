import 'package:flutter/material.dart';

abstract class ConstsUI {
  static const contentPaddings = 16.0;
  static final roundedBorder = BorderRadius.circular(10);

  static const spaceVal = 8.0;
  static const horSpace = SizedBox(width: spaceVal);
  static const verSpace = SizedBox(height: spaceVal);
}
